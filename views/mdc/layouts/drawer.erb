<div class="mdc-layout__drawer">
  <span class="mdc-layout__title"><%= drawer.title %></span>
  <nav class="mdc-navigation">
    <% drawer.menu.items.each do |item| %>
      <a class="mdc-navigation__link" href="<%= item.url %>"><%= item.text %></a>
    <% end %>
  </nav>
</div>