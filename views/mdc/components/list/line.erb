<li class="mdc-list__item<%if line.subtitle%> mdc-list__item--two-line<%end%><%if line.body%> mdc-list__item--three-line<%end%>">
  <span class="mdc-list__item-primary-content">
    <%= erb :"components/list/icon", :locals => {:line => line} %>
    <%= erb :"components/list/avatar", :locals => {:line => line} %>
    <span><%= line.text %></span>
    <% if line.subtitle %><span class="mdc-list__item-sub-title"><%=line.subtitle%></span><%end%>
    <% if line.body %><span class="mdc-list__item-text-body"><%=line.body%></span><%end%>
  </span>
  <span class="mdc-list__item-secondary-content">
  <%= erb :"components/list/info", :locals => {:line => line} %>
  <% line.actions.each do |action| %>
    <%= erb :"components/list/action", :locals => {:action => action} %>
  <% end %>
  </span>

</li>
