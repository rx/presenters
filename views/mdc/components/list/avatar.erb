<% if line.avatar %>
  <% if %w(jpg gif png).include?(line.avatar.avatar&.to_s.split('.').last) %>
    <img class="mdl-list__item-avatar
                <%= 'v-actionable' if line.avatar.events %>"
         id="<%=line.avatar.id%>"
         src="<%= line.avatar.avatar %>"
         <%= erb :"components/event", :locals => {events: line.avatar.events, parent_id: line.avatar.event_parent_id}%> />
  <% else %>
    <i class="material-icons mdl-list__item-avatar
              <%= 'v-actionable' if line.avatar.events %>"
       id="<%=line.avatar.id%>"
       <%= erb :"components/event", :locals => {events: line.avatar.events, parent_id: line.avatar.event_parent_id}%>><%= line.avatar.avatar %></i>
  <% end %>
<% end %>