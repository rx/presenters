<button class="mdc-button mdc-button--raised mdc-js-button dialog-button">Show Dialog</button>

<dialog id="dialog" class="mdc-dialog">
  <h3 class="mdc-dialog__title">MDL Dialog</h3>
  <div class="mdc-dialog__content">
    <p>
      This is an example of the Material Design Lite dialog component.
      Please use responsibly.
    </p>
  </div>
  <div class="mdc-dialog__actions">
    <button type="button" class="mdc-button">Close</button>
    <button type="button" class="mdc-button" disabled>Disabled action</button>
  </div>
</dialog>


<script>
    (function() {
     'use strict';
     var dialogButton = document.querySelector('.dialog-button');
     var dialog = document.querySelector('#dialog');
     if (! dialog.showModal) {
       dialogPolyfill.registerDialog(dialog);
     }
     dialogButton.addEventListener('click', function() {
        dialog.showModal();
     });
     dialog.querySelector('button:not([disabled])')
     .addEventListener('click', function() {
       dialog.close();
     });
   }());
</script>