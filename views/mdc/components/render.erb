<% components.each_with_index do |comp, index| %>
  <%= erb :"components#{scope ? "/#{scope}" : nil}/#{comp.type}", :locals => {comp: comp,
                                                                              components: components, index: index} %>
<% end %>