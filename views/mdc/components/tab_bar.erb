<div id="<%= comp.id %>"
  class="v-tab-bar mdc-tab-bar" role="tablist">
  <div class="mdc-tab-scroller">
    <div class="mdc-tab-scroller__scroll-area">
      <div class="mdc-tab-scroller__scroll-content">

        <% for tab in comp.tabs %>
          <button class="mdc-tab
                    <%= 'mdc-tab--active' if tab.active %>
                    <%= 'mdc-tab--stacked' if tab.stacked %>"
                  role="tab" aria-selected="<%= tab.active %>" tabindex="0"
                  <%= erb :"components/event", :locals => {events: tab.events, parent_id: tab.event_parent_id}%>>
          <span class="mdc-tab__content">
           <% if tab.icon %>
            <span class="mdc-tab__icon material-icons"><%= tab.icon %></span>
            <% end %>
            <span class="mdc-tab__text-label"><%= tab.label %></span>
          </span>
            <span class="mdc-tab-indicator
                    <%= 'mdc-tab-indicator--active' if tab.active %>">
            <span class="mdc-tab-indicator__content mdc-tab-indicator__content--underline"></span>
          </span>
            <span class="mdc-tab__ripple"></span>
          </button>
        <% end %>
      </div>
    </div>
  </div>
</div>