Voom::Presenters.define(:drawers) do
  attach :top_nav
  
  drawer  do
    menu do
      item 'about2', render: 'about'
      item 'components2', render: 'components'
      item 'styles2', render: 'styles'
    end
  end

  attach :grid do
    display 'Drawers'
    
    attach :code, file: __FILE__
  end
end