Voom::Presenters.define('components') do
  attach 'top_nav'
  attach 'component_drawer'

  attach :grid do
    display 'Components'
    grid do
      column 12 do
        icon :keyboard_backspace, position: :left
        subheading 'Select a component'
      end
    end

  end
  attach :code, file: __FILE__

end